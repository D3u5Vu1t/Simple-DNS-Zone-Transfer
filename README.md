# Simple-DNS-Zone-Transfer
Simple Python Script to attempt a DNS-Zone Transfer

This requires that dnspython to be installed which can be installed with the following command: 

pip install dnspython
